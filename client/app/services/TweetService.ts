import { User } from "@/app/models/User";

const LARAVEL_API_URL = process.env.NEXT_PUBLIC_LARAVEL_API_URL;

// Tweet取得
export const getTweets = (accessToken:string) => {

}

// Tweet投稿
export const postTweet = (user:User, message:string) => {

}